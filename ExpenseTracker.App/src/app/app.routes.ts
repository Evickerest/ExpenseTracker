import { Routes } from '@angular/router';
import { LineItemList } from './features/line-items/components/line-item-list/line-item-list';

export const routes: Routes = [
    {
        path: "",
        component: LineItemList
    }
];
